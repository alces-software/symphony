#install it now to avoid accidental install and breakages later
yum -y -e0 install NetworkManager
if ( [ -e /usr/bin/systemctl ] ); then
  systemctl disable NetworkManager
else
  chkconfig NetworkManager off
fi
